{
  "name": "Company IT Overview",
  "style": "Tenant",
  "fields": [
    {
      "name": "MailboxOverview",
      "API": "Exchange",
      "command": "Get-Mailbox",
      "Parameters": {"DetailLevel": "Full"},
      "ExtractFields": ["DisplayName", "PrimarySMTPAddress","RecipientType"],
      "StoreAs": "JSON",
      "FrontendFields": [
          {
            "name": "MailboxOverview",
            "value": "MailboxOverview",
            "formatter": "table"
             "desc": "These are the mailboxes"
          }
      ]
    }
  ]
}
